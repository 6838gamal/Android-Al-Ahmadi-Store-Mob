# FINAL PRODUCTION UPDATE PROMPT V10

## Android Al-Ahmady | أندرويد الأحمدي | شاشات وصيانة الهواتف

مهم جداً:

هذا المشروع موجود بالفعل ويعمل حالياً.

المطلوب هو تحديث وتحسين النظام الحالي فقط.

ممنوع:

* إعادة بناء المشروع من الصفر.
* حذف الجداول الحالية.
* حذف البيانات الحالية.
* تغيير APIs الحالية بشكل يكسر التوافق.
* كسر وظائف المستخدمين الحالية.
* إعادة هيكلة جذرية للمشروع.

نفذ التحديثات بشكل تدريجي وآمن فقط.

━━━━━━━━━━━━━━━━━━
هوية المشروع الرسمية
━━━━━━━━━━━━━━━━━━

الاسم الرسمي الموحد للمشروع:

أندرويد الأحمدي | شاشات وصيانة الهواتف

Android Al-Ahmady | Screens & Mobile Repair

يجب التحقق من ظهوره بشكل موحد في:

* Splash Screen
* Login Screen
* Home Screen
* Drawer
* About Page
* Admin Dashboard
* Invoices
* Reports
* Notifications
* API Documentation

━━━━━━━━━━━━━━━━━━
المراجعة قبل التنفيذ
━━━━━━━━━━━━━━━━━━

قبل تعديل أي ملف:

اعرض:

1. الملفات التي ستتغير.
2. سبب التعديل.
3. تأثير التعديل.
4. خطة التراجع Rollback Plan.

━━━━━━━━━━━━━━━━━━
مصدر البيانات الموحد
━━━━━━━━━━━━━━━━━━

التحقق من أن:

Flutter App
Admin Dashboard
Backend APIs

تستخدم نفس:

* PostgreSQL Database
* Backend API
* Authentication System

يمنع وجود:

* Backend قديم.
* API بديل.
* قاعدة بيانات ثانية.
* روابط إنتاج متعددة غير متزامنة.

يجب توثيق:

Production Base URL

في مكان مركزي واحد فقط.

━━━━━━━━━━━━━━━━━━
إصلاح الثغرات الحرجة
━━━━━━━━━━━━━━━━━━

1. منع تسجيل Staff من التسجيل العام.

السماح بالتسجيل العام لـ:

* Customer فقط.

إنشاء:

* Staff
* Branch Manager
* Admin

من خلال لوحة الإدارة فقط.

━━━━━━━━━━━━━━━━━━

2. حماية بيانات الطلبات.

Customer:

يرى طلباته فقط.

Staff:

يرى الطلبات المصرح له بها فقط.

Branch Manager:

يرى بيانات فرعه فقط.

Admin:

يرى جميع الطلبات.

يجب تطبيق التحقق داخل:

* API
* Service Layer
* Database Filtering

━━━━━━━━━━━━━━━━━━

3. تعطيل Seed Admin في Production.

يعمل فقط في:

Development Environment.

━━━━━━━━━━━━━━━━━━

4. إضافة Rate Limiting.

على:

* Login
* Register
* Password Reset
* Staff Login

━━━━━━━━━━━━━━━━━━

5. تقييد CORS.

منع:

allow_origins=["*"]

في بيئة الإنتاج.

━━━━━━━━━━━━━━━━━━
الأدوار والصلاحيات
━━━━━━━━━━━━━━━━━━

اعتماد:

RBAC + Permissions

التحقق يجب أن يتم على:

* Flutter UI
* Backend API
* Business Logic

وليس فقط إخفاء الأزرار.

━━━━━━━━━━━━━━━━━━

Customer

يستطيع:

* التسجيل.
* تسجيل الدخول.
* تعديل ملفه الشخصي.
* تصفح المنتجات.
* الحجز.
* الطلب.
* تتبع الطلبات.
* الصيانة.
* الضمان.
* الإحالة.
* الإشعارات.

━━━━━━━━━━━━━━━━━━

Staff

يستطيع فقط:

* الرد على الفحص.
* تسجيل الشاشة كمباعة.
* إعادة الشاشة للمخزون.
* تحديث حالات الطلبات.
* تحديث حالات الصيانة.

ولا يستطيع:

* رؤية الأرباح.
* إدارة المستخدمين.
* إدارة الصلاحيات.
* إدارة الإعدادات.
* إدارة النسخ الاحتياطية.

━━━━━━━━━━━━━━━━━━

Branch Manager

يرى ويدير بيانات فرعه فقط.

━━━━━━━━━━━━━━━━━━

Admin

يمتلك تحكماً كاملاً في:

* العملاء.
* الموظفين.
* الفروع.
* الطلبات.
* الصيانة.
* المنتجات.
* الأصناف.
* الصور.
* الإعلانات.
* الإحالات.
* الضمان.
* المحافظ.
* التقارير.
* الإشعارات.
* النسخ الاحتياطية.
* إعدادات النظام.

━━━━━━━━━━━━━━━━━━
إدارة الأصناف
━━━━━━━━━━━━━━━━━━

يجب أن تدعم جميع الأصناف:

* إضافة.
* تعديل.
* حذف.
* أرشفة.
* استعادة.

مع Audit Log كامل.

اعتماد:

Soft Delete

بدلاً من الحذف النهائي.

━━━━━━━━━━━━━━━━━━
إدارة المنتجات والشاشات
━━━━━━━━━━━━━━━━━━

المدير يستطيع:

* إضافة منتج.
* تعديل منتج.
* حذف منتج.
* إيقاف منتج.
* إعادة تفعيل منتج.
* تعديل الأسعار.
* إدارة الصور.
* إدارة المخزون.

━━━━━━━━━━━━━━━━━━

كل شاشة تعتبر:

Unique Inventory Item

وتحتوي على:

* Item ID
* Serial Number
* Category
* Model
* Status
* Images

Status:

* Available
* Reserved
* Sold

━━━━━━━━━━━━━━━━━━
الجلسات وتسجيل الدخول
━━━━━━━━━━━━━━━━━━

إذا كان العميل يمتلك حساباً سابقاً:

يجب أن يتمكن من:

* تسجيل الدخول.
* استعادة بياناته.
* متابعة طلباته السابقة.

ولا يتم إنشاء حساب جديد له عند كل تسجيل دخول.

━━━━━━━━━━━━━━━━━━

دعم:

* Remember Me
* Session Management

━━━━━━━━━━━━━━━━━━
تسجيل الخروج الآمن
━━━━━━━━━━━━━━━━━━

عند تسجيل الخروج:

* حذف Access Token.
* حذف Refresh Token (إن وجد).
* حذف بيانات الجلسة المحلية.
* تنظيف البيانات الحساسة من الذاكرة.

بعد تسجيل الخروج:

يمنع الوصول لأي شاشة محمية باستخدام:

* زر الرجوع.
* Deep Links.
* Cached Navigation.

ولا يمكن العودة إلا بعد تسجيل الدخول من جديد.

━━━━━━━━━━━━━━━━━━
سلامة البيانات
━━━━━━━━━━━━━━━━━━

التحقق من عدم تكرار:

* User ID
* Phone Number
* Item ID
* Order Number
* Referral Code
* Serial Number
* Device Fingerprint

مع:

* Unique Constraints
* Database Indexes

━━━━━━━━━━━━━━━━━━
الإحالة و Device Fingerprint
━━━━━━━━━━━━━━━━━━

ربط Flutter مع نظام الإحالة.

إرسال:

Referral Code

أثناء التسجيل.

تفعيل:

Device Fingerprint

وإرساله أثناء:

* التسجيل.
* تسجيل الدخول.
* الإحالة.

━━━━━━━━━━━━━━━━━━
إشعارات الطلبات
━━━━━━━━━━━━━━━━━━

إنشاء إشعار تلقائي عند:

* استلام الطلب.
* تأكيد الطلب.
* التجهيز.
* الشحن.
* التسليم.
* الإلغاء.

━━━━━━━━━━━━━━━━━━
التقارير
━━━━━━━━━━━━━━━━━━

Branch Manager:

يرى تقارير فرعه فقط.

Admin:

يرى جميع التقارير.

━━━━━━━━━━━━━━━━━━
التدقيق النهائي
━━━━━━━━━━━━━━━━━━

بعد الانتهاء:

قم بإجراء Full Audit جديد.

أنشئ تقريراً يحتوي على:

* ما تم إصلاحه.
* ما تم التحقق منه.
* ما يزال ناقصاً.
* المخاطر المتبقية.
* نسبة الجاهزية للإطلاق.

ولا تعتبر المشروع جاهزاً للإنتاج إلا بعد اجتياز جميع البنود المذكورة أعلاه دون كسر أي وظيفة حالية.
